rules:
  unpinned-uses:
    config:
      policies:
        actions/*: ref-pin
        github/*: ref-pin
        devantler-tech/*: ref-pin
        "*": hash-pin
  secrets-outside-env:
    config:
      allow:
        - APP_PRIVATE_KEY
        - NUGET_API_KEY
        - CODECOV_TOKEN
