rules:
  unpinned-uses:
    config:
      policies:
        actions/*: ref-pin
        github/*: ref-pin
        devantler-tech/*: ref-pin
        "*": hash-pin
  secrets-outside-env:
    config:
      allow:
        - app-private-key
        - nuget-api-key
        - codecov-token
